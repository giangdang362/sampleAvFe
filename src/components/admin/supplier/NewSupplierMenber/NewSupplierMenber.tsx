import { Box } from "@mui/material";
import React from "react";

export type NewSupplierMenberProps = {
  t: (t: string) => string;
};
export default function NewSupplierMenber({}: NewSupplierMenberProps): React.JSX.Element {
  return <Box></Box>;
}
